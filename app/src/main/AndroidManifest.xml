<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devfest.its.its_devfest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Welcome_slider">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".NewPostActivity" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PageProjects"
            android:label="@string/title_activity_page_projects"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PostsActivity"
            android:label="@string/title_activity_posts"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".LoginDev" />
        <activity android:name=".MainAuth" />
        <activity android:name=".Loginste"></activity>
    </application>

</manifest>